import authRequests from "./auth.requests";
import usersRequests from "./users.requests";
import transactionRequests from "./transaction.requests";

export default {
  auth: authRequests,
  users: usersRequests,
  transactions: transactionRequests,
};
